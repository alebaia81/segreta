# ─────────────────────────────────────────────────────────────────────────────
# public/_redirects — Segreta Style
# Cloudflare Pages SPA Routing
#
# Redirige tutte le route non-file verso index.html (React Router / stato URL).
# Equivalente dell'Apache .htaccess per Cloudflare Pages.
# Sintassi: https://developers.cloudflare.com/pages/configuration/redirects/
# ─────────────────────────────────────────────────────────────────────────────

# SPA catch-all is now handled natively in wrangler.toml with not_found_handling = "single-page-application"
# /*    /index.html    200
